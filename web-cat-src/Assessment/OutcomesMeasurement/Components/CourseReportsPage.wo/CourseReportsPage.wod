OutcomeRepetition: WORepetition {
	list = outcomePairStats;
	item = anOutcomeStat;
}

SelectCourse: WCFilteringSelect {
    list          = allCourses;
    selection     = currCourse;
    item          = aCourse;
    autoComplete   = true;
    invalidMessage = "Invalid course selection";
}