Drop the lib, mode, and theme folders from CodeMirror into this folder.

Then, create a compressed version of the CodeMirror Javascript (for example,
here: http://codemirror.net/doc/compress.html, select every JS file) and drop
it into this folder, with the name "codemirror-compressed.js".
