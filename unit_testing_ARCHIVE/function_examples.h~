#include <stdio.h>

int product( int x, int y ); 

int main ( int argc, char* argv[] ) { 
  int temp; 
  temp = product(2, 3); 
  printf("%d", temp); 
  return 0; 
} 


int product(x, y) {
  int z; 
  z = x*y; 
  return z; 
} 


